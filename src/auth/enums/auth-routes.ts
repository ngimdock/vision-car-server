export enum AuthRoute {
  auth = 'auth',
  register = 'register',
  login = 'login',
  logout = 'logout',
  confirmEmail = 'confirm-email',
  changeEmail = 'change-email',
  resetPassword = 'reset-password',
}
