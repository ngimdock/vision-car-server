export enum AuthRoute {
  auth = 'auth',
  register = 'register',
  login = 'login',
  logout = 'logout',
  verifyEmail = 'email/verify',
  changeEmail = 'email/change',
  resetPassword = 'password/reset',
}
